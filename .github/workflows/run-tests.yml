name: Server Unit Tests

on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    #   - uses: actions/checkout@v2
    #   - uses: actions/setup-node@v1
    #     with:
    #       node-version: "16.8.x"
    #   - run: |
    #       cd server
    #       npm ci
    #       npm run test
